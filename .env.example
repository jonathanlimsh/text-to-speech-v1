SERVICE=auto
INPUT_SUBDIR=
INPUT_DIR=
FORMATS=wav
LANGUAGE_ID=en
RECURSIVE=true
NO_TIMESTAMP=false
SPLIT_CHUNKS=false
OVERWRITE=true
DEVICE=auto
TRIM_SILENCE=true
TRIM_TAIL_ONLY=true
SILENCE_TOP_DB=40
SILENCE_MIN_DUR=0.3
SILENCE_PAD_MS=30
PROCESSED_DIR_NAME=processed
# Optional: override detected timezone offset (format +/-HHMM)
TZ_OFFSET=
# Hugging Face token for private models
# HF_TOKEN=
